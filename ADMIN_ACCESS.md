## Private Booking Monitor

- Internal dashboard lives at `/studio-monitor` and is **not linked** anywhere in the public UI.
- When deployed to GitHub Pages it resolves to: `https://JesseRod329.github.io/Moe/studio-monitor/`.
- The page loads live data directly from Supabase using the same anon key as the main site, so only share the URL with trusted team members.
- Bookmark the URL instead of adding navigation links to keep it hidden. Rename the folder if you ever need to rotate the location.

